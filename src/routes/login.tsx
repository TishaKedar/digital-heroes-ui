import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowRight, Eye, Sparkles } from "lucide-react";
import { Brand } from "@/components/digital-heroes";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";

export const Route = createFileRoute("/login")({ head: () => ({ meta: [{ title: "Log in — Digital Heroes" }, { name: "description", content: "Log in to your Digital Heroes member dashboard." }, { property: "og:title", content: "Log in — Digital Heroes" }, { property: "og:description", content: "Access your golf scores, draws, and charity impact." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary" }] }), component: LoginPage });

function LoginPage() { return <div className="grid min-h-screen lg:grid-cols-[1.05fr_.95fr]"><aside className="hero-wash hidden p-12 lg:flex lg:flex-col lg:justify-between"><Brand/><div className="max-w-lg"><span className="grid size-12 place-items-center rounded-xl bg-primary text-primary-foreground"><Sparkles /></span><h1 className="mt-8 text-5xl font-extrabold leading-tight">Welcome back to the club.</h1><p className="mt-5 text-lg leading-8 text-muted-foreground">Your scores, monthly draw, and charitable impact are waiting.</p></div><p className="text-sm font-semibold text-muted-foreground">Play well. Do good.</p></aside><main className="flex items-center justify-center bg-card px-5 py-12"><div className="w-full max-w-md"><div className="mb-12 lg:hidden"><Brand/></div><p className="text-xs font-extrabold uppercase text-primary">Member access</p><h2 className="mt-3 text-3xl font-extrabold">Log in to your account</h2><p className="mt-3 text-muted-foreground">Good to see you again.</p><form className="mt-9 grid gap-5" onSubmit={(e)=>e.preventDefault()}><label className="grid gap-2 text-sm font-bold">Email<Input type="email" placeholder="alex@example.com" /></label><label className="grid gap-2 text-sm font-bold">Password<div className="relative"><Input type="password" placeholder="Enter your password" className="pr-10"/><Eye className="pointer-events-none absolute right-3 top-3 size-5 text-muted-foreground"/></div></label><div className="text-right"><a href="#" className="text-sm font-bold text-primary">Forgot password?</a></div><Button size="lg" className="w-full">Log in <ArrowRight /></Button></form><p className="mt-8 text-center text-sm text-muted-foreground">New to Digital Heroes? <Link to="/signup" className="font-bold text-primary">Create an account</Link></p><Button asChild variant="ghost" className="mx-auto mt-4 flex"><Link to="/">Back to home</Link></Button></div></main></div> }